import { BACKEND_URL, CURRENCY, DELIVERY_FEE, SITE_NAME } from '@/config/constants';

export const appConfig = {
  siteName: SITE_NAME,
  currency: CURRENCY,
  deliveryFee: DELIVERY_FEE,
  backendUrl: BACKEND_URL,
};